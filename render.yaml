services:
  - type: web
    name: jate-server
    env: node
    plan: free
    buildCommand: |
      cd client && npm install && npm run build &&
      cd ../server && npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
    regions:
      - oregon